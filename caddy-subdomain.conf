# App Template - Subdomain routing
app-template.doughughes.net {
    forward_auth localhost:8000 {
        uri /verify
    }
    reverse_proxy localhost:7999

    handle_errors {
        @401 expression {http.error.status_code} == 401
        redir @401 https://www.doughughes.net/login 302
    }
}
